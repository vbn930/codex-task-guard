interface:
  display_name: "Task Guard"
  short_description: "Use fresh quota snapshots for measured task phases"
  default_prompt: "Use $task-guard to run this task with authoritative quota snapshots, measured phases, consistent checkpoints, and same-thread resume."
policy:
  allow_implicit_invocation: true
