interface:
  display_name: "Task Guard"
  short_description: "Guard long Codex tasks across quota resets"
  default_prompt: "Use $task-guard to protect this multi-phase task across quota checks, checkpointing, and same-thread resume."
policy:
  allow_implicit_invocation: true
